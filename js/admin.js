function ActionSessionLogout(session_id) {
    console.log(`Logouted ${session_id}`);
}

function ActionAccountEditBalance(id) {
    console.log(`Edited Balanced ${id}`);
}

function ActionAccountLineDelete(id) {
    console.log(`Deleted ${id}`);
}